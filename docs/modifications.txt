# objectif : créer le parsing des points d'interets

## Backend : 
Créer une base de donnée dédiée aux points d'intérêt : /data/poi.sqlite
Fichier docs\liste_poi.md contient :
    - liste des points d'interet (points 1 à 3),
    - liste consolidée des POI (point 4) à prévoir dans poi.sqliteliste consolidée des POI à prévoir dans poi.sqlite
    - améliorer ces deux listes et adapter au modèle de l'application :
        - fusioner les points 1, 2 et 3
        - catégoriser correctement les deux listes 
Améliorer le modèle de parsing des données : 
    - modèle à améliorer : docs\parsing_donnees.md
    - modèle de stockage des csv avant implémentation dans la poi.sqlite :
        - stockage dans /data/csv/ par poi (par exemple /data/csv/pharmacies.csv)
    - création d'un script python qui parse tous les csv dans /data/csv vers le poi.sqlite dans le backend (/backend/scripts/)
    - création d'un script qui intègre une fonction qui permet de géocoder les adresses des POI en lat et lon 
    - stratégie de fonctionnement : 
        - ne pas parser les CSV directement dans la map
        - importer les sources dans SQLite
        - normaliser
        - géocoder
        - servir la carte depuis des tables indexées : R*Tree pour les recherches par bbox
Source des données : 
    - chercher les sources des différends poi docs\liste_poi.md
    - exemple donnée pour les pharmacies dans docs\parsing_donnees.md, à éttendre pour les autres poi 
    - stocker les csv bruts dans /data/csv/ sous le nom du poi (exemple : pharmacies.csv)

## Sur la page /map : 
- sur la map quand on met la souris sur un point, il y a un rectangle qui apparait et il est très bien : 
    - garder l'ui de ce rectangle,
    - définir les informations qui s'affichent dans ce rectangle (nom, adresse, finess, etc)